version: "3"
services:
  app:
    build: .
    restart: always
    ports:
      - "3000:3000"
    depends_on:
      - redis
    environment:
      - "REDIS_URL=redis://redis:6379"
  redis:
    image: "redis:alpine"
